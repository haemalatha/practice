<h1>Listing Schools</h1>
<%= link_to 'New school', new_path %>

<table>
  <tr>
    <th>Name</th>
    <th>Address</th>
  </tr>

  <% @schools.each do |school| %>
      <tr>
        <td><%= school.name %></td>
        <td><%= school.address %></td>
      </tr>
  <% end %>
</table>